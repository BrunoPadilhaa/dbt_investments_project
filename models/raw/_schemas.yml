version: 2

models:
  - name: raw_asset
    description: "Cleaned Portuguese trades data"
    columns:
      - name: asset_code
        description: "Unique identifier for each trade"
        tests:
          - unique
          - not_null 