name: 'INVESTMENTS'
version: '1.0.0'
config-version: 2

profile: 'INVESTMENTS'

models:
  INVESTMENTS:
    staging:
      +schema: STAGING
      +materialized: table
    intermediate:
      +schema: INTERMEDIATE
      +materialized: table
    marts:
      +schema: PROD
      +materialized: table

seeds:
  INVESTMENTS:
    +schema: RAW

snapshots:
  INVESTMENTS:
    +schema: PROD
